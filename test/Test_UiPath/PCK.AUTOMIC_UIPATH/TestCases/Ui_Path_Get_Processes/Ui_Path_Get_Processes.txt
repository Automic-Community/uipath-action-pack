*** Settings ***
Suite Setup       Startup
Suite Teardown    Teardown
Force Tags        get_processes
Test Template     Get Processes Template
Library           com.automic.robot.itpa.ItpaLibrary
Library           String
Library           DateTime
Resource          ../../Resources/messages.txt
Resource          ../../Resources/keywords.txt
Resource          ../../Resources/variables.txt
Resource          ../../Resources/actions.txt

*** Test Cases ***    OK_NOK                                     ProcessName        LatestVersion      Properties       Filter           OrderBy       Top         Skip        Include_Count    Expand
Get Processes with all valid data
                      [Tags]                                     main_scenarios
                      OK                                         ${Process_Name}    YES                ${EMPTY}         ${EMPTY}         ${OrderBy}    ${Top}      ${Skip}     YES              ${Expand}
                      #Get Processes with valid Property Name
                      #                                          OK                 ${Process_Name}    YES              ${Properties}    ${EMPTY}      ${EMPTY}    ${EMPTY}    ${Skip}          NO           ${EMPTY}

Get Processes with Invalid or non existing Property
                      NOK                                        ${Process_Name}    YES                Invalid          ${EMPTY}         ${OrderBy}    ${Top}      ${Skip}     NO               ${EMPTY}

Get Processes with Invalid or non existing Filter
                      NOK                                        ${Process_Name}    YES                ${Properties}    Invalid          ${OrderBy}    ${Top}      ${Skip}     NO               ${EMPTY}

Get Processes with all fields blank
                      OK                                         ${EMPTY}           YES                ${EMPTY}         ${EMPTY}         ${EMPTY}      ${EMPTY}    ${Skip}     NO               ${EMPTY}

*** Keywords ***
Startup
    Log    *******Start Test "Get_Processes "*******
    Log    *******Connect to AE*******
    Connect AE

Get Processes Template
    [Arguments]    ${OK_NOK}    ${ProcessName}    ${LatestVersion}    ${Properties}    ${Filter}    ${OrderBy}
    ...    ${Top}    ${Skip}    ${Include_Count}    ${Expand}
    [Documentation]    This test is to test the feature of Get Processes .
    ...    -${OK_NOK}: Boolean value indicate if the action is ENDED_OK or ENDED_NOT_OK. Value of this variable should be OK or NOK.
    ...    -${ProcessName}: This field specifies Process Name you want filter.
    ...    -${LatestVersion}: This field specifies if you want to fetch release with latest version.
    ...    -${Properties}: This field specifies the list of properties as comma separated to include in the response. E.g. ProcessName,TimeStamp.
    ...    -${Filter}: This field specifies the filters to be applied for the results, based on a Boolean condition. E.g. ProcessName eq 'SalesProcess.
    ...    -${OrderBy}: This field specifies specify the property to be used in ordering. E.g. TimeStamp desc
    ...    -${Top}: This field specifies if the result should include only the first N items.
    ...    -${Include_Count}: This field specifies the count user wants to includes.
    ...    -${Skip}: This field specifies if the result should skip the first N items.
    ...    -${Expand}: This field specifies related entities to be inlined..
    Generate String
    Init Action    ${_GET_PROCESSES_ACTION}
    Connect To UiPath
    Run Keyword If    '${ProcessName}' == 'Invalid'    Action Set    &UC4RB_UIP_PROC_NAME#    ${Generated_Name}_${Time}    ELSE    Action Set
    ...    &UC4RB_UIP_PROC_NAME#    ${ProcessName}
    Action Set    &UC4RB_UIP_LATEST_VERSION#    ${LatestVersion}
    Run Keyword If    '${Properties}' == 'Invalid'    Action Set    &UC4RB_UIP_PROP#    ${Generated_Name}_${Time}    ELSE    Action Set
    ...    &UC4RB_UIP_PROP#    ${Properties}
    Run Keyword If    '${Filter}' == 'Invalid'    Action Set    &UC4RB_UIP_FILTER#    ${Generated_Name}_${Time}    ELSE    Action Set
    ...    &UC4RB_UIP_FILTER#    ${Filter}
    Run Keyword If    '${OrderBy}' == 'Invalid'    Action Set    &UC4RB_UIP_ORD_BY#    ${Generated_Name}_${Time}    ELSE    Action Set
    ...    &UC4RB_UIP_ORD_BY#    ${OrderBy}
    Run Keyword If    '${Top}' == 'Invalid'    Action Set    &UC4RB_UIP_TOP#    ${Generated_Name}_${Time}    ELSE    Action Set
    ...    &UC4RB_UIP_TOP#    ${Top}
    Run Keyword If    '${Skip}' == 'Invalid'    Action Set    &UC4RB_UIP_SKIP#    ${Generated_Name}_${Time}    ELSE    Action Set
    ...    &UC4RB_UIP_SKIP#    ${Skip}
    Run Keyword If    '${Include_Count}'    Action Set    &UC4RB_UIP_INC_CNT#    ${Include_Count}
    Run Keyword If    '${Expand}' == 'Invalid'    Action Set    &UC4RB_UIP_EXPAND#    ${Generated_Name}_${Time}    ELSE    Action Set
    ...    &UC4RB_UIP_EXPAND#    ${Expand}
    Action Execute
    Run Keyword If    '${OK_NOK}' == 'OK'    Assert Success    ELSE    Assert Failure
    Run Keyword If    '${OK_NOK}' == 'OK'    Action Object Variable Count Value Should Be    &UC4RB_UIP_PROCESS_ID_COUNT#
    Run Keyword If    '${OK_NOK}' == 'OK' and '&UC4RB_UIP_PROCESS_ID_COUNT#' > '0'    Action Object Variable Should Be Found    &UC4RB_UIP_PROCESS_ID_LIST#
    Run Keyword If    '${OK_NOK}' == 'NOK'    Action Object Variable Should Not Be Found    &UC4RB_UIP_PROCESS_ID_LIST#

Teardown
    Log    *******End Test "Get_Processes"*******
